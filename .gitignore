# Dependências (rebuildar com npm install)
node_modules/

# Build Next.js (rebuildar com npm run dev / build)
.next/
out/
build/

# Logs e PIDs temporários
*.log
.dev-server.pid
.dev-server.current
.dev-worker.pid
.claude/scheduled_tasks.lock
image (1).png

# Segredos locais (nunca versionar credenciais reais)
.env
.env.*
!.env.example

# Dados locais/sensÃ­veis
prisma/*.db
prisma/*.db-*
uploads/
*.ofx
*.pdf
NU_*.csv
*UnifiedTransaction*.csv

# TypeScript gerado
*.tsbuildinfo
next-env.d.ts

# Cópias locais
"ERP AMAZON2/"
ERP AMAZON2/
